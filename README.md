# pyGRaTer alpha has moved

This repository has been superseded by the public `pyGrater` repository:

<https://github.com/prioletp/pyGrater>

Please use the new repository for installation, issues, updates, and future
development.

```bash
git clone https://github.com/prioletp/pyGRaTer.git
cd pyGRaTer
pip install -e .
```

The old `pyGRaTer_alpha` repository is kept only as a historical pointer.
